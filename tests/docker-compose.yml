services:
  test:
    image: python:3.12
    working_dir: /workspace
    volumes:
      - ..:/workspace
      - pip-cache:/root/.cache/pip
    command: sh -c "pip install -r tests/requirements-test.txt -q && pytest tests/test_rodret.py -v"

volumes:
  pip-cache:
